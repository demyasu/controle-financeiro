$kind: http-request
url: "{{base_url}}/reset_password/{{reset_token}}"
method: POST
headers:
  Content-Type: application/x-www-form-urlencoded
body:
  type: urlencoded
  content:
    password: "{{new_password}}"
    confirm_password: "{{new_password}}"
order: 8000
