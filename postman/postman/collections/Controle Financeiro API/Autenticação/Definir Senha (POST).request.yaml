$kind: http-request
url: "{{base_url}}/register/{{registration_token}}"
method: POST
headers:
  Content-Type: application/x-www-form-urlencoded
body:
  type: urlencoded
  content:
    username: "{{username}}"
    password: "{{new_password}}"
    confirm_password: "{{new_password}}"
order: 5000
